apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "recipe-service.fullname" . }}-test-connection"
  labels:
    {{- include "recipe-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "recipe-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
